'use strict';

const express = require('express');
const app = express();
//express documentation requires the previous two lines

app.listen(3000), function() {
  console.log('listening on port 3000');
  
});